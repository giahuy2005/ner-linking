"""Deterministic rules ported from the canonical NER notebook.

Submodules are intentionally not imported eagerly: routing consumes the
two-pass region schema, while two-pass consumes clinical rules.
"""
